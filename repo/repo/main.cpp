#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include "Header.h"
using namespace std;
int main(){
	string name = "premier_league.csv";
	string s;
	int num = 0;
	CommandNum(s, name, num);

}